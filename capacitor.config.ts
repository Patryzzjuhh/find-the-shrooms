import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'find.the.shrooms',
  appName: 'find-the-shrooms',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
